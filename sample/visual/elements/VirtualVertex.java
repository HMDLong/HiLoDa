package sample.visual.elements;

import javafx.scene.control.Button;

public class VirtualVertex extends Button {
    public VirtualVertex(double x, double y){
        super();
        setLayoutX(x);
        setLayoutY(y);
    }
}
